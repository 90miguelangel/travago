<%= form_for resource_type do |f| %>
  <% if resource_type.errors.any? %>
    <ul>
      <% resource_type.errors.full_messages.each do |m| %>
      <li> <%= m %></li>
      <% end %>
    </ul>
  <% end %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <br/>
  <%= f.submit "Send" %>
<% end %>