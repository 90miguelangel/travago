<%= form_for traveler do |f| %>
  <% if traveler.errors.any? %>
    <ul>
      <% traveler.errors.full_messages.each do |m| %>
      <li> <%= m %></li>
      <% end %>
    </ul>
  <% end %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <br/>
  <%= f.submit "Send" %>
<% end %>