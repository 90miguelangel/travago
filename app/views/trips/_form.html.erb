<%= form_for trip do |f| %>
  <% if trip.errors.any? %>
    <ul>
      <% trip.errors.full_messages.each do |m| %>
      <li> <%= m %></li>
      <% end %>
    </ul>
  <% end %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <br/>
  <%= f.submit "Create" %>
<% end %>